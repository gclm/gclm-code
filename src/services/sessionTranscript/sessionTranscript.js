// Session transcript service stub.
// The full session transcript subsystem is not included in this build.

/**
 * Write a message segment to the remote session transcript.
 * No-op in this build variant.
 */
export async function writeSessionTranscriptSegment(messages) {
  // No-op
}
