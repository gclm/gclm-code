// Proactive command stub.
// The full proactive command is not included in this build.
const proactiveCommand = {
  name: 'proactive',
  description: 'Toggle proactive mode (stub).',
  execute: async () => {},
};
export default proactiveCommand;
