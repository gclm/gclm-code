/**
 * Session transcript stub.
 * The full transcript writing is gated on feature('KAIROS').
 * This stub ensures the code compiles when KAIROS is enabled.
 */

/**
 * Write a transcript segment to the session transcript file.
 * Stub implementation - transcript writing is not yet implemented in this build.
 * @param {any} messages
 */
export async function writeSessionTranscriptSegment(messages) {
  // No-op in this build variant
}
