/**
 * SendUserFileTool stub.
 * The full implementation is gated on feature('KAIROS').
 * This stub ensures the code compiles when KAIROS is enabled.
 */
export const SendUserFileTool = {
  name: 'SendUserFile',
  prompt: 'Send a file to the user for review or feedback.',
};
