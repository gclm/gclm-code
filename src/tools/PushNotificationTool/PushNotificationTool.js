/**
 * PushNotificationTool stub.
 * The full implementation is gated on feature('KAIROS_PUSH_NOTIFICATION').
 * This stub ensures the code compiles when KAIROS_PUSH_NOTIFICATION is enabled.
 */
export const PushNotificationTool = {
  name: 'PushNotification',
  prompt: 'Send a push notification to the user.',
};
